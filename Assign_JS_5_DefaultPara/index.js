// Destructuring assignment with an object
const person = { name: "Siddharth", age: 34, city: "Bhilwara" };

// Destructuring the object into variables
const { name, age, city } = person;

console.log(name); 
console.log(age);   
console.log(city);  